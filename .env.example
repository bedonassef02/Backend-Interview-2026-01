# 🚀 Server / Network Configuration
PORT=3000
# Comma-separated or '*' for wildcards. Set to your frontend URL in production
CORS_ORIGIN=*

# 🛡️ Authentication Keys
# Minimun 16 characters for production
JWT_SECRET=your-super-sensitive-jwt-secret-key-at-least-16-chars
# Time format: 1h, 30m, 7d
JWT_EXPIRATION=1h
# Static API key for programmatic / bulk upload access
API_KEY=your-secure-static-api-key-8-chars-min

# 🚨 Rate Limiting (Global Defaults)
# TTL in seconds
THROTTLE_TTL=60
# Max requests per TTL
THROTTLE_LIMIT=10

# 👤 Admin Credentials (for UI dashboard login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin-secure-password